import { CheckIsAddressDirective } from './check-is-address.directive';

describe('CheckIsAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckIsAddressDirective();
    expect(directive).toBeTruthy();
  });
});
