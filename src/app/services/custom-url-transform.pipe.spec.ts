import { CustomUrlTransformPipe } from './custom-url-transform.pipe';

describe('CustomUrlTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomUrlTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
